{
  "name": "react-maps",
  "version": "0.3.2",
  "description": "",
  "author": "Daniel Cousens",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dcousens/react-maps.git"
  },
  "bugs": {
    "url": "https://github.com/dcousens/react-maps/issues"
  },
  "homepage": "https://github.com/dcousens/react-maps",
  "keywords": [
    "react-maps"
  ],
  "main": "lib/index.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "build": "babel --out-dir lib/ src/",
    "build-examples": "mkdir -p examples/build && browserify -t babelify examples/src/example1.js --outfile examples/build/example1.js",
    "prepublish": "npm run build",
    "standard": "standard",
    "test": "npm run standard",
    "watch": "babel --watch --out-dir lib/ src/",
    "watch-examples": "mkdir -p examples/build && watchify -v -t babelify examples/src/example1.js --outfile examples/build/example1.js"
  },
  "standard": {
    "ignore": [
      "lib/"
    ]
  },
  "dependencies": {
    "blacklist": "^1.1.2",
    "react": "^0.13.3"
  },
  "devDependencies": {
    "babel": "*",
    "babelify": "*",
    "browserify": "*",
    "standard": "*",
    "watchify": "*"
  }
}
